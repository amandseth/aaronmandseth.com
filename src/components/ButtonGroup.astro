---
interface Props {
	extraClasses?: string;
}

const { extraClasses } = Astro.props;
---

<ul
	class:list={[
		"inline-flex list-none flex-row gap-4 rounded-full px-2.5 py-2 leading-none",
		extraClasses,
	]}
>
	<slot />
</ul>
