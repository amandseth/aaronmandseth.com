---
interface Props {
	href: string;
	extraClasses?: string;
}

const { href, extraClasses } = Astro.props;
---

<li>
	<a
		class:list={[
			"inline-block rounded-full px-4 py-2 transition-colors",
			extraClasses,
		]}
		href={href}
	>
		<slot />
	</a>
</li>
